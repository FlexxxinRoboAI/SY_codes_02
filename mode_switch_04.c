// Check grade using marks (switch with range logic)
#include <stdio.h>

int main() {
    int choice;
    printf("Enter your choice: ");
    scanf("%d", &choice);

    switch(choice) {
        case 1: printf("Executing Check grade using marks (switch with range logic)...\n"); break;
        default: printf("Default case for Check grade using marks (switch with range logic)\n");
    }
    return 0;
}
